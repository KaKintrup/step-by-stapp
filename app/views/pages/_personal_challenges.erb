<% @personal_participation = ChallengeParticipation.all%>
<ul>

    <% @personal_participation.each do  |participation|%>
        <% if participation.user = current_user %>
          <% #TODO see all challenges also the one you  picked  %>
                <li>
                    <%= participation.challenge.title %>
                    <div class="btn-primary">
                        <%= link_to "See this challenge", challenge_path(participation.challenge) %>
                    </div>
                </li>
        <% end %>

    <% end %>
</ul>
